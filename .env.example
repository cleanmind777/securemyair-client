# Copy this file to .env and fill in values.
# Do not commit .env to version control.
# Only variables prefixed with REACT_APP_ are embedded in the build (Create React App).

# ------------------------------------------------------------------------------
# Backend API
# ------------------------------------------------------------------------------
# Base URL for the PHP client API (no trailing slash).
# If set, use this in src/index.js: axios.defaults.baseURL = process.env.REACT_APP_API_URL;
REACT_APP_API_URL=http://localhost/php/client

# Production example:
# REACT_APP_API_URL=https://api.securemyair.com/client

# ------------------------------------------------------------------------------
# Optional: public config
# ------------------------------------------------------------------------------
# App display name (e.g. for titles or branding).
# REACT_APP_APP_NAME=SecureMyAir
